# 用户注册登录功能 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为图书馆座位预约系统添加用户名密码注册登录功能，替换现有体验账号切换模式，注册数据写入 StudentUsers 表，登录时验证用户名和密码哈希。

**Architecture:** 扩展现有 `StudentUser` 模型（+ Username、PasswordHash），新增 `AccountController` 处理注册/登录/登出，复用现有 Session key（UserId/UserName）保持下游 Controller 兼容。密码使用 `PasswordHasher<StudentUser>` PBKDF2 哈希存储。

**Tech Stack:** ASP.NET Core MVC 9、EF Core 9、SQL Server LocalDB、Bootstrap 5.3

## Global Constraints

- 所有密码使用 `Microsoft.AspNetCore.Identity.PasswordHasher<StudentUser>` 哈希，不存明文
- 用户名 3-50 字符（字母/数字/下划线），唯一索引
- 密码最少 6 位
- 登录错误不区分"用户不存在"和"密码错误"，统一返回"用户名或密码错误"
- Session key `UserId`(int) / `UserName`(string) 保持不变
- 移除原有 5 个体验账号种子数据
- 不使用 ASP.NET Core Identity（仅用 PasswordHasher）

---

### Task 1: 扩展 StudentUser 模型 + 更新 DbContext

**Files:**
- Modify: `src/LibrarySeatReservation.Web/Models/StudentUser.cs`
- Modify: `src/LibrarySeatReservation.Web/Data/AppDbContext.cs`
- Modify: `src/LibrarySeatReservation.Web/Data/DbInitializer.cs`

**Interfaces:**
- Produces: `StudentUser` 新增 `Username` (string)、`PasswordHash` (string?) 属性
- Produces: `StudentUsers` 表新增 `IX_StudentUsers_Username` 唯一索引

- [ ] **Step 1: 修改 StudentUser.cs**

```csharp
namespace LibrarySeatReservation.Web.Models;

/// <summary>
/// 用户实体 — 对应 StudentUsers 表（含注册/登录信息）
/// </summary>
public class StudentUser
{
    public int Id { get; set; }
    public string Name { get; set; } = string.Empty;
    public string Username { get; set; } = string.Empty;
    public string? PasswordHash { get; set; }

    // Navigation
    public ICollection<Reservation> Reservations { get; set; } = new List<Reservation>();
}
```

- [ ] **Step 2: 更新 AppDbContext — 添加 Username 唯一索引**

在 StudentUsers 的配置块中添加 Username 属性配置和唯一索引：

```csharp
// === StudentUsers ===
modelBuilder.Entity<StudentUser>(entity =>
{
    entity.ToTable("StudentUsers");
    entity.HasKey(e => e.Id);
    entity.Property(e => e.Name).HasMaxLength(50).IsRequired();
    entity.Property(e => e.Username).HasMaxLength(50).IsRequired();
    entity.Property(e => e.PasswordHash).HasMaxLength(200);
    entity.HasIndex(e => e.Username, "IX_StudentUsers_Username").IsUnique();
});
```

- [ ] **Step 3: 修改 DbInitializer — 移除体验账号种子数据**

```csharp
using LibrarySeatReservation.Web.Models;

namespace LibrarySeatReservation.Web.Data;

public static class DbInitializer
{
    public static void Initialize(AppDbContext db)
    {
        // 清理旧版体验账号（Username 为空的旧数据）
        var oldUsers = db.StudentUsers.Where(u => string.IsNullOrEmpty(u.Username)).ToList();
        if (oldUsers.Any())
        {
            db.StudentUsers.RemoveRange(oldUsers);
            db.SaveChanges();
        }

        // 如果已有正式用户，跳过
        if (db.StudentUsers.Any()) return;

        // === 种子数据：12 个座位（3 区域 × 4 座位） ===
        var seats = new List<Seat>
        {
            new() { SeatNumber = "A-01", Area = "一楼大厅", Floor = "1F", Status = "Available" },
            new() { SeatNumber = "A-02", Area = "一楼大厅", Floor = "1F", Status = "Available" },
            new() { SeatNumber = "A-03", Area = "一楼大厅", Floor = "1F", Status = "Available" },
            new() { SeatNumber = "A-04", Area = "一楼大厅", Floor = "1F", Status = "Available" },
            new() { SeatNumber = "B-01", Area = "二楼阅览室", Floor = "2F", Status = "Available" },
            new() { SeatNumber = "B-02", Area = "二楼阅览室", Floor = "2F", Status = "Available" },
            new() { SeatNumber = "B-03", Area = "二楼阅览室", Floor = "2F", Status = "Available" },
            new() { SeatNumber = "B-04", Area = "二楼阅览室", Floor = "2F", Status = "Available" },
            new() { SeatNumber = "C-01", Area = "三楼自习区", Floor = "3F", Status = "Available" },
            new() { SeatNumber = "C-02", Area = "三楼自习区", Floor = "3F", Status = "Available" },
            new() { SeatNumber = "C-03", Area = "三楼自习区", Floor = "3F", Status = "Available" },
            new() { SeatNumber = "C-04", Area = "三楼自习区", Floor = "3F", Status = "Available" },
        };
        db.Seats.AddRange(seats);
        db.SaveChanges();
    }
}
```

- [ ] **Step 4: 生成迁移**

```bash
dotnet ef migrations add AddUserAuth --project src/LibrarySeatReservation.Web
```

- [ ] **Step 5: 验证 build**

```bash
dotnet build src/LibrarySeatReservation.Web 2>&1
```

预期输出：`Build succeeded.` 0 error, 0 warning

---

### Task 2: 创建 AccountController + ViewModels

**Files:**
- Create: `D:\AIWeb\src\LibrarySeatReservation.Web\Controllers\AccountController.cs`
- Create: `D:\AIWeb\src\LibrarySeatReservation.Web\Models\RegisterViewModel.cs`
- Create: `D:\AIWeb\src\LibrarySeatReservation.Web\Models\LoginViewModel.cs`

**Interfaces:**
- Produces: `GET/POST /Account/Register` — 注册表单
- Produces: `GET/POST /Account/Login` — 登录表单
- Produces: `POST /Account/Logout` — 登出
- Consumes: `StudentUser.Username`、`StudentUser.PasswordHash`（从 Task 1）
- Uses: `PasswordHasher<StudentUser>` 进行密码哈希和验证
- Session: 设置 `UserId` / `UserName`

- [ ] **Step 1: 创建 RegisterViewModel.cs**

```csharp
using System.ComponentModel.DataAnnotations;

namespace LibrarySeatReservation.Web.Models;

public class RegisterViewModel
{
    [Required(ErrorMessage = "用户名不能为空")]
    [StringLength(50, MinimumLength = 3, ErrorMessage = "用户名长度为 3-50 个字符")]
    [RegularExpression(@"^[a-zA-Z0-9_]+$", ErrorMessage = "用户名只能包含字母、数字和下划线")]
    [Display(Name = "用户名")]
    public string Username { get; set; } = string.Empty;

    [Required(ErrorMessage = "姓名不能为空")]
    [StringLength(50, MinimumLength = 1, ErrorMessage = "姓名长度为 1-50 个字符")]
    [Display(Name = "姓名")]
    public string Name { get; set; } = string.Empty;

    [Required(ErrorMessage = "密码不能为空")]
    [StringLength(100, MinimumLength = 6, ErrorMessage = "密码至少 6 位")]
    [DataType(DataType.Password)]
    [Display(Name = "密码")]
    public string Password { get; set; } = string.Empty;

    [DataType(DataType.Password)]
    [Display(Name = "确认密码")]
    [Compare("Password", ErrorMessage = "两次密码输入不一致")]
    public string ConfirmPassword { get; set; } = string.Empty;
}
```

- [ ] **Step 2: 创建 LoginViewModel.cs**

```csharp
using System.ComponentModel.DataAnnotations;

namespace LibrarySeatReservation.Web.Models;

public class LoginViewModel
{
    [Required(ErrorMessage = "用户名不能为空")]
    [Display(Name = "用户名")]
    public string Username { get; set; } = string.Empty;

    [Required(ErrorMessage = "密码不能为空")]
    [DataType(DataType.Password)]
    [Display(Name = "密码")]
    public string Password { get; set; } = string.Empty;
}
```

- [ ] **Step 3: 创建 AccountController.cs**

```csharp
using Microsoft.AspNetCore.Identity;
using Microsoft.AspNetCore.Mvc;
using LibrarySeatReservation.Web.Data;
using LibrarySeatReservation.Web.Models;

namespace LibrarySeatReservation.Web.Controllers;

public class AccountController : Controller
{
    private readonly AppDbContext _db;
    private readonly PasswordHasher<StudentUser> _passwordHasher = new();

    public AccountController(AppDbContext db)
    {
        _db = db;
    }

    // ========== 注册 ==========

    [HttpGet]
    public IActionResult Register()
    {
        // 已登录用户跳转首页
        if (HttpContext.Session.GetInt32("UserId").HasValue)
            return RedirectToAction("Index", "Home");
        return View();
    }

    [HttpPost]
    public IActionResult Register(RegisterViewModel model)
    {
        if (!ModelState.IsValid)
            return View(model);

        // 检查用户名唯一性
        var exists = _db.StudentUsers.Any(u => u.Username == model.Username);
        if (exists)
        {
            ModelState.AddModelError("Username", "该用户名已被注册");
            return View(model);
        }

        // 创建用户
        var user = new StudentUser
        {
            Username = model.Username,
            Name = model.Name,
            PasswordHash = null // 先赋 null，下面哈希后再赋值
        };
        user.PasswordHash = _passwordHasher.HashPassword(user, model.Password);

        _db.StudentUsers.Add(user);
        _db.SaveChanges();

        // 自动登录
        HttpContext.Session.SetInt32("UserId", user.Id);
        HttpContext.Session.SetString("UserName", user.Name);

        TempData["SuccessMessage"] = "注册成功，欢迎使用！";
        return RedirectToAction("Index", "Home");
    }

    // ========== 登录 ==========

    [HttpGet]
    public IActionResult Login()
    {
        if (HttpContext.Session.GetInt32("UserId").HasValue)
            return RedirectToAction("Index", "Home");
        return View();
    }

    [HttpPost]
    public IActionResult Login(LoginViewModel model)
    {
        if (!ModelState.IsValid)
            return View(model);

        var user = _db.StudentUsers.FirstOrDefault(u => u.Username == model.Username);
        if (user == null || user.PasswordHash == null)
        {
            ModelState.AddModelError("", "用户名或密码错误");
            return View(model);
        }

        var result = _passwordHasher.VerifyHashedPassword(user, user.PasswordHash, model.Password);
        if (result == PasswordVerificationResult.Failed)
        {
            ModelState.AddModelError("", "用户名或密码错误");
            return View(model);
        }

        // 登录成功 — 设置 Session
        HttpContext.Session.SetInt32("UserId", user.Id);
        HttpContext.Session.SetString("UserName", user.Name);

        // 如果有来源页，跳回来源页
        if (!string.IsNullOrEmpty(model.ReturnUrl) && Url.IsLocalUrl(model.ReturnUrl))
            return Redirect(model.ReturnUrl);

        TempData["SuccessMessage"] = "登录成功！";
        return RedirectToAction("Index", "Home");
    }

    // ========== 登出 ==========

    [HttpPost]
    public IActionResult Logout()
    {
        HttpContext.Session.Remove("UserId");
        HttpContext.Session.Remove("UserName");
        TempData["SuccessMessage"] = "已退出登录";
        return RedirectToAction("Index", "Home");
    }
}
```

注意：LoginViewModel 需要添加一个 ReturnUrl 属性（可选）。

- [ ] **Step 4: 为 LoginViewModel 添加 ReturnUrl 属性**

```csharp
public class LoginViewModel
{
    // ... 原有属性 ...

    [HiddenInput]
    public string? ReturnUrl { get; set; }
}
```

---

### Task 3: 创建 Register.cshtml 和 Login.cshtml 视图

**Files:**
- Create: `src/LibrarySeatReservation.Web/Views/Account/Register.cshtml`
- Create: `src/LibrarySeatReservation.Web/Views/Account/Login.cshtml`

**Interfaces:**
- Consumes: `RegisterViewModel`、`LoginViewModel` 从 Task 2
- Uses: 现有 Bootstrap 组件和自定义 CSS classes（`.login-card`、`.card-custom-static`、`.form-custom`、`.btn-custom-primary`）

- [ ] **Step 1: 创建 Register.cshtml**

```html
@model RegisterViewModel
@{
    ViewData["Title"] = "注册";
}

<div class="login-card">
    <div class="card-custom-static">
        <div class="text-center mb-4">
            <div style="font-size:40px;margin-bottom:8px;">📝</div>
            <h4>注册新账号</h4>
            <p class="text-secondary" style="font-size:14px;">创建账号后即可预约座位</p>
        </div>

        <form asp-controller="Account" asp-action="Register" method="post">
            <div asp-validation-summary="ModelOnly" class="alert alert-custom-error mb-3" role="alert"></div>

            <div class="mb-3">
                <label asp-for="Username" class="form-label-custom"></label>
                <input asp-for="Username" class="form-custom w-100" placeholder="3-50位字母、数字或下划线" />
                <span asp-validation-for="Username" class="form-custom-error"></span>
            </div>
            <div class="mb-3">
                <label asp-for="Name" class="form-label-custom"></label>
                <input asp-for="Name" class="form-custom w-100" placeholder="请输入真实姓名" />
                <span asp-validation-for="Name" class="form-custom-error"></span>
            </div>
            <div class="mb-3">
                <label asp-for="Password" class="form-label-custom"></label>
                <input asp-for="Password" class="form-custom w-100" placeholder="至少 6 位密码" />
                <span asp-validation-for="Password" class="form-custom-error"></span>
            </div>
            <div class="mb-3">
                <label asp-for="ConfirmPassword" class="form-label-custom"></label>
                <input asp-for="ConfirmPassword" class="form-custom w-100" placeholder="再次输入密码" />
                <span asp-validation-for="ConfirmPassword" class="form-custom-error"></span>
            </div>
            <button type="submit" class="btn btn-custom-primary w-100">注 册</button>
        </form>

        <div class="text-center mt-3">
            <span style="font-size:13px;color:var(--color-text-secondary);">已有账号？</span>
            <a asp-controller="Account" asp-action="Login" style="font-size:13px;">立即登录</a>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
```

- [ ] **Step 2: 创建 Login.cshtml**

```html
@model LoginViewModel
@{
    ViewData["Title"] = "登录";
}

<div class="login-card">
    <div class="card-custom-static">
        <div class="text-center mb-4">
            <div style="font-size:40px;margin-bottom:8px;">🔑</div>
            <h4>用户登录</h4>
            <p class="text-secondary" style="font-size:14px;">使用注册的账号登录系统</p>
        </div>

        <form asp-controller="Account" asp-action="Login" method="post">
            <div asp-validation-summary="ModelOnly" class="alert alert-custom-error mb-3" role="alert"></div>
            <input type="hidden" asp-for="ReturnUrl" />

            <div class="mb-3">
                <label asp-for="Username" class="form-label-custom"></label>
                <input asp-for="Username" class="form-custom w-100" placeholder="请输入用户名" />
                <span asp-validation-for="Username" class="form-custom-error"></span>
            </div>
            <div class="mb-3">
                <label asp-for="Password" class="form-label-custom"></label>
                <input asp-for="Password" class="form-custom w-100" placeholder="请输入密码" />
                <span asp-validation-for="Password" class="form-custom-error"></span>
            </div>
            <button type="submit" class="btn btn-custom-primary w-100">登 录</button>
        </form>

        <div class="text-center mt-3">
            <span style="font-size:13px;color:var(--color-text-secondary);">还没有账号？</span>
            <a asp-controller="Account" asp-action="Register" style="font-size:13px;">立即注册</a>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
```

---

### Task 4: 修改 _Layout.cshtml 导航栏

**Files:**
- Modify: `src/LibrarySeatReservation.Web/Views/Shared/_Layout.cshtml`

**Interfaces:**
- Consumes: `HttpContext.Session.GetInt32("UserId")` 判断登录状态
- Consumes: `HttpContext.Session.GetString("UserName")` 显示用户名
- Produces: 导航栏右端登录/注册/退出链接

- [ ] **Step 1: 替换导航栏右侧的体验账号切换为登录/注册/退出**

找到这行代码（约第 34-50 行）：
```html
<!-- 体验账号切换 -->
<div class="d-flex align-items-center gap-2">
    <span class="user-display">当前：<strong>@(ViewBag.CurrentUser ?? "未选择")</strong></span>
    ...
</div>
```

替换为：
```html
<!-- 用户登录状态 -->
<div class="d-flex align-items-center gap-2">
    @{
        var loggedInUserId = Context.Session.GetInt32("UserId");
        var loggedInUserName = Context.Session.GetString("UserName");
    }
    @if (loggedInUserId.HasValue)
    {
        <span class="user-display">你好，<strong>@loggedInUserName</strong></span>
        <form asp-controller="Account" asp-action="Logout" method="post" class="d-inline">
            <button type="submit" class="btn btn-custom-outline btn-custom-sm"
                    style="color:#fff;border-color:rgba(255,255,255,0.4);padding:4px 12px;font-size:13px;">退出</button>
        </form>
    }
    else
    {
        <a asp-controller="Account" asp-action="Login"
           class="btn btn-custom-outline btn-custom-sm"
           style="color:#fff;border-color:rgba(255,255,255,0.4);padding:4px 12px;font-size:13px;">登录</a>
        <a asp-controller="Account" asp-action="Register"
           class="btn btn-custom-primary btn-custom-sm"
           style="padding:4px 12px;font-size:13px;">注册</a>
    }
</div>
```

需要在文件顶部添加 Session 扩展方法的 using：
```html
@using Microsoft.AspNetCore.Http
```

---

### Task 5: 修改 HomeController 和首页视图

**Files:**
- Modify: `src/LibrarySeatReservation.Web/Controllers/HomeController.cs`
- Modify: `src/LibrarySeatReservation.Web/Views/Home/Index.cshtml`

**Interfaces:**
- Consumes: `HttpContext.Session.GetInt32("UserId")`、`GetString("UserName")` 判断登录状态
- Removes: `SwitchUser` action
- Removes: 体验账号列表卡片

- [ ] **Step 1: 修改 HomeController — 移除 SwitchUser 和体验账号加载**

```csharp
using System.Diagnostics;
using Microsoft.AspNetCore.Mvc;
using LibrarySeatReservation.Web.Data;
using LibrarySeatReservation.Web.Models;

namespace LibrarySeatReservation.Web.Controllers;

public class HomeController : Controller
{
    private readonly AppDbContext _db;
    private readonly ILogger<HomeController> _logger;

    public HomeController(AppDbContext db, ILogger<HomeController> logger)
    {
        _db = db;
        _logger = logger;
    }

    /// <summary>
    /// 首页 — 根据登录状态显示不同内容
    /// </summary>
    public IActionResult Index()
    {
        var userId = HttpContext.Session.GetInt32("UserId");
        ViewBag.IsLoggedIn = userId.HasValue;
        ViewBag.CurrentUser = HttpContext.Session.GetString("UserName") ?? "";
        return View();
    }

    [ResponseCache(Duration = 0, Location = ResponseCacheLocation.None, NoStore = true)]
    public IActionResult Error()
    {
        return View(new ErrorViewModel { RequestId = Activity.Current?.Id ?? HttpContext.TraceIdentifier });
    }

    [ResponseCache(Duration = 0, Location = ResponseCacheLocation.None, NoStore = true)]
    public IActionResult HandleStatusCode(int code)
    {
        ViewBag.StatusCode = code;
        return code switch
        {
            404 => View("NotFound"),
            403 => View("Forbidden"),
            _ => View("Error", new ErrorViewModel { RequestId = HttpContext.TraceIdentifier })
        };
    }
}
```

- [ ] **Step 2: 修改 Index.cshtml — 移除体验账号列表**

```html
@{
    ViewData["Title"] = "首页";
}

<div class="row justify-content-center">
    <div class="col-lg-8">
        @if (ViewBag.IsLoggedIn)
        {
            <!-- 已登录欢迎 -->
            <div class="text-center mb-4">
                <h1 class="display-6 fw-bold" style="color:var(--color-primary);">图书馆座位预约</h1>
                <p class="text-secondary">欢迎回来，开始预约座位吧</p>
            </div>

            <div class="card-custom-static mb-4">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <h5 class="mb-1">你好，<strong>@ViewBag.CurrentUser</strong> 👋</h5>
                        <p class="text-secondary mb-0" style="font-size:14px;">可前往座位列表预约座位</p>
                    </div>
                    <div>
                        <a class="btn btn-custom-primary" asp-controller="Seats" asp-action="Index">查看座位</a>
                    </div>
                </div>
            </div>

            <div class="row mt-4 g-3">
                <div class="col-md-6">
                    <div class="card-custom-static text-center">
                        <div style="font-size:32px;margin-bottom:8px;">🪑</div>
                        <h6>浏览座位</h6>
                        <p class="text-secondary" style="font-size:13px;">查看各区域座位空闲情况</p>
                        <a class="btn btn-custom-primary btn-custom-sm" asp-controller="Seats" asp-action="Index">去看看</a>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="card-custom-static text-center">
                        <div style="font-size:32px;margin-bottom:8px;">📋</div>
                        <h6>我的预约</h6>
                        <p class="text-secondary" style="font-size:13px;">查看和取消我的预约记录</p>
                        <a class="btn btn-custom-primary btn-custom-sm" asp-controller="Reservation" asp-action="My">去看看</a>
                    </div>
                </div>
            </div>
        }
        else
        {
            <!-- 未登录欢迎 -->
            <div class="text-center mb-4">
                <h1 class="display-6 fw-bold" style="color:var(--color-primary);">图书馆座位预约</h1>
                <p class="text-secondary">登录后即可预约图书馆座位</p>
            </div>

            <div class="card-custom-static text-center mb-4">
                <div style="font-size:48px;margin-bottom:12px;">🏛️</div>
                <h5 class="mb-3">欢迎使用图书馆座位预约系统</h5>
                <p class="text-secondary" style="font-size:14px;max-width:360px;margin:0 auto 20px;">
                    请登录或注册账号后浏览座位、预约座位。
                </p>
                <div class="d-flex justify-content-center gap-3">
                    <a class="btn btn-custom-primary" asp-controller="Account" asp-action="Login">登录</a>
                    <a class="btn btn-custom-outline" asp-controller="Account" asp-action="Register">注册</a>
                </div>
            </div>
        }
    </div>
</div>
```

---

### Task 6: 添加 _ValidationScriptsPartial 引用 + 更新 _ViewImports

**Files:**
- Modify: `src/LibrarySeatReservation.Web/Views/_ViewImports.cshtml`（如果需要）
- Check: `src/LibrarySeatReservation.Web/Views/Shared/_ValidationScriptsPartial.cshtml` 是否存在

**Interfaces:**
- Consumes: ASP.NET Core 客户端校验

- [ ] **Step 1: 检查 _ValidationScriptsPartial 是否存在**

```bash
Test-Path "src/LibrarySeatReservation.Web/Views/Shared/_ValidationScriptsPartial.cshtml"
```

如果不存在，创建它：

```html
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validate/1.21.0/jquery.validate.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validation-unobtrusive/4.1.0/jquery.validate.unobtrusive.min.js"></script>
```

- [ ] **Step 2: 验证 _ViewImports.cshtml 已有 @addTagHelper**

当前内容已包含 `@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers`，无需改动。

---

### Task 7: 生成迁移 + build 验证

**Files:**
- N/A（运行时产物）

- [ ] **Step 1: 确保项目可编译**

```bash
dotnet build src/LibrarySeatReservation.Web 2>&1
```

修复任何编译错误。

- [ ] **Step 2: 生成 EF Core 迁移**

```bash
dotnet ef migrations add AddUserAuth --project src/LibrarySeatReservation.Web
```

预期输出：`Done. To undo this action, use 'ef migrations remove'`

---

### Task 8: 运行浏览器验证注册/登录流程

**Files:**
- N/A（运行时验证）

- [ ] **Step 1: 启动应用 & 打开浏览器**

```bash
dotnet run --project src/LibrarySeatReservation.Web
```

- [ ] **Step 2: 验证流程**

使用 Playwright 验证：
1. 访问首页 http://localhost:5207 — 应显示未登录页面，有"登录"和"注册"按钮
2. 点击"注册" — 进入注册页面
3. 填写表单（用户名: testuser, 姓名: 测试用户, 密码: 123456）— 提交
4. 注册成功，自动登录，跳转首页，显示"你好，测试用户"
5. 点击"退出" — 回到未登录首页
6. 点击"登录" — 进入登录页面
7. 填写 testuser / 123456 — 登录成功，跳转首页
8. 点击"座位列表" — 可正常查看座位
9. 尝试注册已存在的用户名 — 应显示"该用户名已被注册"
10. 尝试错误密码登录 — 应显示"用户名或密码错误"
