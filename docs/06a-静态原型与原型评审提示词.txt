你现在扮演“前端原型设计师 + 页面搭建助理”，帮助一名软件实习生根据《页面卡与UI规范》生成《图书馆座位预约系统》的静态页面原型，并补齐原型评审文档。

落盘要求：
1. 读取输入文件：docs/05-页面卡与UI规范.md
2. 本轮结果必须直接写入：prototype/static-v1/、docs/06-静态原型与原型评审.md、prototype/review-1/原型评审清单.md
3. 如已有静态原型，则按本轮要求覆盖更新或补出 static-v2
4. 不要只在对话中展示结果

输入材料：
- docs/05-页面卡与UI规范.md

固定要求如下：
1. 本轮只做静态页面，不接数据库。
2. 本轮只放合理假数据，不做真实业务逻辑。
3. 使用 Bootstrap。
4. 用户端按手机优先设计。
5. 管理端按电脑端后台优先设计。
6. 页面总数按 9 页推进。
7. 不能做成“纯文本 + 链接目录页”，每个核心页面都必须有真实布局和组件。
8. 必须额外补一份自定义样式文件，不能只停留在 Bootstrap 默认效果。
9. 页面之间必须能通过相对链接点击跳转。
10. 至少展示成功状态、空状态、异常提示中的两类页面效果。

你要输出：
1. prototype/static-v1/ 下的核心静态页面原型
2. docs/06-静态原型与原型评审.md
3. prototype/review-1/原型评审清单.md

其中：
- 原型说明文档要写清页面生成情况、假数据说明、未接入逻辑、下一步进入什么设计开发工作
- 原型说明文档还要写清：本轮采用的视觉方向、用户端与管理端各自的界面风格、共享组件和自定义样式文件位置
- 原型评审清单必须包含：
1. 核心用户流程是否完整
2. 页面跳转逻辑是否清楚
3. 空状态和错误状态是否考虑
4. 手机端和桌面端是否区分
5. 哪些问题必须在接后端前修改

页面质量要求：
1. 用户端页面至少要有顶部区、主要内容区、操作按钮区和底部安全留白，更像真实手机页面。
2. 管理端页面至少要有导航区、筛选区、列表或统计区、操作区，更像真实后台。
3. 列表页要有表格或卡片，不允许只写几行说明文字。
4. 表单页要有真实输入项、按钮、提示文案和状态反馈，不允许只摆字段名称。
5. 统计页要有数据卡片、趋势区或状态分布区，不允许只有标题。
6. 要有统一色彩、统一圆角、统一间距和统一按钮风格，不能每页像不同项目拼起来。
7. 如果视觉效果太弱，优先减少页面数量也不要把 9 页都做成很简陋的壳。

如果已有审计意见，再根据审计意见补出 prototype/static-v2/。
