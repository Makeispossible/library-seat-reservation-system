# 13 — 用户端主链路开发审计报告

> 审计时点：Sprint 1 round 1 完成（提交 `6a64bc4`）
> 审计基线：docs/05、docs/08、docs/09、docs/12、项目任务板
> 审计对象：README.md、docs/13、项目任务板、src/

---

## A. 总体判断

**Sprint 1 round 1（用户端主链路验证）整体通过，3 个任务卡全部完成并验证，达到阶段最低完成线。** 实现与 docs/05、08、09 的设计基本一致，未发现偏离设计规范的 P0/P1 问题。存在 4 项 P3 级建议优化项和 1 处文档笔误，不阻塞进入下一阶段。

---

## B. 发现的问题清单

### B1. 对照 docs/05 页面卡与 UI 规范

| # | 问题 | 级别 | 涉及文档/文件 | 说明 |
|---|------|------|-------------|------|
| F1 | 空状态标题与设计文档不完全一致 | P3 | docs/05 页面卡 2 ↔ Seats/Index.cshtml | docs/05 规定空状态显示"该区域暂无座位"；代码 h6 显示"暂无座位"，p 显示"该区域暂无座位，请调整筛选条件。"。语义等值但视觉优先级不同 |
| F2 | 座位列表页 "异常提示" 未实现 | P3 | docs/05 页面卡 2 ↔ SeatsController.cs | docs/05 规定"后端查询失败 → 显示'加载失败，请稍后重试'"。当前未对 DB 查询做 try-catch，异常时走默认错误页。约束同 docs/09 第 2.2 节"ASP.NET Core 默认错误页"，决策一致，但不满足 docs/05 异常提示要求 |
| F3 | 首页"最重要的操作"是下拉框，但代码同时提供了 button 列表 | P3 | docs/05 页面卡 1 ↔ Home/Index.cshtml | docs/05 描述"最重要的操作：体验账号下拉切换"，代码同时实现了下拉框和 button 列表。功能上更完整，但与设计文档的主操作描述有扩展偏差 |

### B2. 对照 docs/08 数据库设计

未发现偏差。全部符合：

| 检查项 | 结果 |
|--------|------|
| 3 表结构（StudentUsers / Seats / Reservations） | ✅ 完全一致 |
| 字段名、类型、约束 | ✅ 完全一致 |
| 4 个索引 | ✅ 已建 |
| 2 个外键约束（CASCADE） | ✅ 已建 |
| 种子数据（5 学生 + 12 座位） | ✅ 与文档完全一致 |
| 状态字面量（Available / Maintenance / Pending / Cancelled） | ✅ 严格一致 |

### B3. 对照 docs/09 关键链路详细设计

| # | 问题 | 级别 | 涉及文档/文件 | 说明 |
|---|------|------|-------------|------|
| F4 | `today` 使用的类型偏离 docs/09 伪代码 | P3 | docs/09 第 2.2 节 ↔ 多个 Controller | docs/09 伪代码用 `DateTime.Today`，但代码用 `DateOnly.FromDateTime(DateTime.Today)`。这是因为 Model 中 `Date` 字段类型用了 `DateOnly`（匹配 SQL Server `date` 类型），这是正确的实现决策，非文档背离 |

### B4. 文档自身问题

| # | 问题 | 级别 | 涉及文件 | 说明 |
|---|------|------|---------|------|
| F5 | 截图表格第二行描述截断 | P4 | docs/13-用户端主链路开发记录.md 第 150 行 | 原文为"同上，Session 持"，缺"久化"二字，疑似笔误 |

### B5. 技术债务延续

| # | 问题 | 级别 | 来源 | 说明 |
|---|------|------|------|------|
| TD-01 | `favicon.ico` 404 | P3 | docs/12 遗留 | 已知问题，docs/13 已记录 |
| TD-02 | 导航栏当前页不高亮 | P3 | docs/13 自提 | 已记录 |
| TD-03 | `LibrarySeatReservation.Web.styles.css` 404 | P4 | 无影响 | 已记录 |

---

## C. 必改项

**无 P0/P1 必改项。**

所有发现均为 P3/P4 建议优化或已记录的技术债务，不阻塞下一阶段。

---

## D. 建议优化项

| 编号 | 建议 | 级别 | 建议处理时机 | 说明 |
|------|------|------|------------|------|
| R1 | Seats/Index.cshtml 空状态 h6 改为"该区域暂无座位" | P3 | Sprint 1 下一轮 | 与 docs/05 页面卡 2 保持完全一致 |
| R2 | 给 SeatsController.Index() 加 try-catch，DB 异常时走错误消息而非默认错误页 | P3 | Sprint 2 | 对齐 docs/05 页面卡 2 的"异常提示"要求 |
| R3 | docs/13 第 150 行"Session 持"补全为"Session 持久化" | P4 | 下一轮 doc 修订 | 笔误修复 |
| R4 | 考虑将 docs/05 页面卡 1 的"最重要的操作"从"体验账号下拉切换"更新为"下拉切换 + 按钮切换" | P4 | 下一轮 doc 修订 | 对齐实际实现 |

---

## E. 能否进入下一步"管理端、权限与状态回流"

### E1 逐项核查 Sprint 1 阶段最低完成线

| 标准 | 状态 | 证据 |
|------|------|------|
| 首页可显示 | ✅ 通过 | Playwright 截图 + Snapshot |
| 体验账号可切换并持久化 | ✅ 通过 | SwitchUser POST → Session 写入 → 页面显示"学生A" |
| 座位列表可展示 12 个座位带筛选 | ✅ 通过 | 12 座位网格 + 3 区域 + 3 楼层筛选 + 空状态 |
| 座位详情可显示带状态标签和预约按钮守卫 | ✅ 通过 | A-01 详情页显示"空闲"+"预约此座位" |

### E2 Sprint 1 实际完成范围 vs 全部 Sprint 1 范围

| 模块 | Sprint 1 全部范围 | 本轮已完成 | 状态 |
|------|------------------|-----------|------|
| 用户首页 | Controller+View 就绪 → 验证通过 | ✅ 已验证 | ✅ |
| 座位列表、筛选、动态状态 | Controller+View 就绪 → 验证通过 | ✅ 已验证 | ✅ |
| 座位详情、预约按钮守卫 | Controller+View 就绪 → 验证通过 | ✅ 已验证 | ✅ |
| **预约提交 + 冲突校验** | Controller+View 就绪 → 验证通过 | ❌ 未验证 | 🔲 |
| **我的预约 + 取消** | Controller+View 就绪 → 验证通过 | ❌ 未验证 | 🔲 |

### E3 结论

- [x] 已达到阶段最低完成线（首页显示 + 账号切换 + 座位列表 + 座位详情）
- [x] Sprint 1 实现与设计规范一致（docs/05 / 08 / 09）
- [x] 任务板记录真实完整（3 张任务卡已完成，完成率 100%）
- [x] README 已增量更新（已验证项标记为 ✅，未验证项保留 🔲 并标注"待验证"）
- [x] 未将前序阶段问题扩大为当前必改项（已记录为技术债务或优化建议）

**结论：✅ 满足条件，允许进入下一步（Sprint 2 — 预约提交 + 我的预约 + 取消预约，或 Sprint 1 下一轮对预约功能进行验证）。**

建议优先级：下一轮优先验证 ReservationController（Create POST / My / Cancel）的运行时正确性，再进入管理端开发。

---

## 附：审计路径对照

| 审计路径要求 | 是否覆盖 | 说明 |
|------------|---------|------|
| 1. 审计对象：README、docs/13、任务板、src/ | ✅ | 全部覆盖 |
| 2. 审计结果写入 docs/13-用户端主链路开发-审计.md | ✅ | 本文档 |
| 3. 不直接修改原文件或代码 | ✅ | 仅审计，无代码修改 |
| 4. 以 docs/05/08/09/12 为基线，不重做前阶段全量审计 | ✅ | 仅对照当前阶段 3 任务卡的实现 |
| 5~13 逐项检查 | ✅ | 见上各节 |

*审计日期：2026-07-13*
*审计基线提交：6a64bc4*
